#include <stdio.h>
int main (void)
{
   int a [17]; // make an array with 10 elements
   int i;      // make an index variable
   // Print all possible indexes (useless!!!): 
   for (i = 0 ; i < 17 ; ++i)
      printf("%d ",i);
   return 0;
}
