// print-ascii-for-f-test.c
#include <stdio.h>
int main (void)
{
   char x = 'f';
   printf("ASCII for f in decimal = %d\n",   (int)x);
   printf("ASCII for f in hex     = 0x%X\n", (int)x);
   return 0;
}