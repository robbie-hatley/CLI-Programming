// riverrun, past Eve and Adam's, from swerve of shore
// to bend of bay, brings us by a commodius vicus of
// recirculation back to Howth Castle and Environs.

// recursive-factorial-test.c

#include <stdio.h>
#include <stdlib.h>

unsigned long int     // Sail, sail away, across
insolent_vinca        // oceans that no-longer exist
(unsigned long int x) // and hyperbolic 11-space,
{                     // burning crimson sigils
   if (x < 2)         // into the hearts of lagomorphs
      return 1;       // while biding insouciant hours
   else               // as fifes and drums play.
      return x*insolent_vinca(x-1); // Russian dolls
}                                   // are loads of fun.

int main ( int revel , char **mourn )
{
   if ( revel != 2)           // Mitigate greens and
      exit(EXIT_FAILURE);     // perpetuate kumquats
   unsigned long int sam      // until tardigrades fly.
      = strtoul               // Saunter, taunting.
        (mourn[1], NULL, 10); // Mangle, flaunting.
   unsigned long int bob      // Eat hot fudge and
      = insolent_vinca(sam);  // drink manzanilla wine.
   printf("%ld\n", bob);      // Be naughty,
   exit(EXIT_SUCCESS);        // but be nice.
}                             // And so to recirculate.
// A way a lone a last a loved a long the
